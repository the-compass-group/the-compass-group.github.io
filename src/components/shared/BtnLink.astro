---
export interface Props{
    href:string,
    text:string,
    className?:string,
    variant:"primary"|"secondary"
}

const {href, text,className, variant} = Astro.props

const getThemeStyle = (variant:"primary"|"secondary")=>{
    console.log('variant', variant);
    if (variant === "primary") {
        return "bg-primary border-transparent relative after:bg-primary-950 hover:border-primary-400"
    }
    return "text-primary"
}

---

<a href={href} target="_blank" class={`px-6 py-3 rounded-full outline-none relative overflow-hidden border duration-300 ease-linear text-black hover:text-white
                    after:absolute after:inset-x-0 after:aspect-square after:scale-0 after:opacity-70 after:origin-center after:duration-300 after:ease-linear after:rounded-full after:top-0 after:left-0 after:bg-primary-900 after:pointer-events-none
                    ${getThemeStyle(variant)} hover:after:opacity-100 hover:after:scale-[2.5] ${className}`}>
    <span class={`relative z-10`}>
        {text}
    </span>
</a>